# Kids Wear

## Run web app

```bash
serve
```

## Run as mobile app (identical UI in WebView)

```bash
npm install
npm run mobile:sync
```

Open native projects:

```bash
npm run mobile:android
npm run mobile:ios
```

After any web changes, run:

```bash
npm run mobile:sync
```
