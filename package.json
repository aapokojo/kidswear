{
  "name": "kidswear",
  "version": "1.0.0",
  "description": "$ serve",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "mkdir -p dist && rsync -a --delete --exclude .git --exclude node_modules --exclude dist --exclude android --exclude ios --exclude .vercel ./ dist/",
    "mobile:sync": "npm run build:web && npx cap sync",
    "mobile:android": "npm run mobile:sync && npx cap open android",
    "mobile:ios": "npm run mobile:sync && npx cap open ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^7.6.2"
  },
  "devDependencies": {
    "@capacitor/android": "^7.6.2",
    "@capacitor/cli": "^7.6.2",
    "@capacitor/ios": "^7.6.2"
  }
}
